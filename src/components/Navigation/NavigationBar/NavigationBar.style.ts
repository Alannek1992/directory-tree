import styled from "styled-components";

const AppInstanceNavigationIcon = styled.div`
  margin: 0 10px;
  padding: 2px;
  border: 2px solid #ffa500;
  border-radius: 10px;
  background: white;
  :hover {
    color: white;
    background: black;
    cursor: pointer;
  }
`;

export const Styled = {
  AppInstanceNavigationIcon
};
